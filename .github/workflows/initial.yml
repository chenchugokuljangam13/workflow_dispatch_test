# # name: CI
# on:
#   pull_request:
#     types: [edited, opened, reopened, synchronize]
# # for testing i will use this workflows on push action
#   push:
#     branches:
#       - main

# jobs:
#   build-and-test:

#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Use Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18.x'
      
#       - name: Install dependencies
#         run: npm i
      
#       - name: Run test cases
#         run: npm run unit

      
name: CI

on:
  pull_request:
    types: [edited, opened, reopened, synchronize]


jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'

      - name: Install dependencies
        run: npm i

      - name: Run test cases
        run: npm run unit

      - name: Get PR info and send to Supabase
        if: github.event_name == 'pull_request'
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"
          COMMIT_SHA="${{ github.event.pull_request.head.sha }}"
          ID="5566b5a9-87e5-468f-bc38-5823b57496e8"
          curl -X PATCH "${{ secrets.SUPABASE_URL }}/rest/v1/candidate_assessment?id=eq.${ID}" \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"pr_name\": \"${PR_TITLE}\", \"sha\": \"${COMMIT_SHA}\"}"